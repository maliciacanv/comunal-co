import { FilterEmpresasPipe } from './filter-empresas.pipe';

describe('FilterEmpresasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEmpresasPipe();
    expect(pipe).toBeTruthy();
  });
});
